<select id="getExpiredSysEmpNo" resultType="String">
    SELECT HIUT.ACCOUNT
FROM HCP_IDM_USR_TBL HIUT
WHERE HIUT.ACCOUNT_STATUS = '1'
  AND NOT EXISTS (
      SELECT 1
      FROM IDM_USR_TBL IUT
      WHERE IUT.ACCOUNT = HIUT.ACCOUNT
  )

UNION ALL

SELECT IUT.ACCOUNT
FROM IDM_USR_TBL IUT
WHERE IUT.ACCOUNT_STATUS = '3'
</select>
